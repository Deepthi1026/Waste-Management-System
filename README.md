# Waste-Management-System
A simple Python-based Waste Management System that tracks bins, updates their status, and generates collection requests when bins are full. It allows adding bins, reporting full bins, processing requests, and displaying current bin status using object-oriented programming concepts.
